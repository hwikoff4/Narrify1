{
  "buildCommand": "pnpm install && pnpm turbo run build --filter=narrify-dashboard",
  "outputDirectory": "apps/dashboard/.next",
  "installCommand": "pnpm install",
  "framework": "nextjs"
}
